# G30信标扫描工具 Android版 - 项目说明

## 项目概述

本项目是 WinForm 程序 TestG30 的 Android 移植版本，用于通过 USB OTG 连接 G30 硬件设备，扫描并记录蓝牙信标(Beacon)数据。

## 与原 WinForm 程序的对应关系

| WinForm (TestG30) | Android (G30Scanner) | 说明 |
|-------------------|----------------------|------|
| `Form1.cs` | `MainActivity.java` | 主界面 |
| `SerialPort` | `UsbSerialManager` | 串口通信 |
| `BeaconItem` | `BeaconItem.java` | 数据模型 |
| `ComboBox` (cboComPorts) | `Spinner` | 设备选择 |
| `ListBox` (listBox1) | `ListView` | 信标列表 |
| `Timer` (timer1) | `Handler` | 定时刷新 |
| `TextBox` (txtReceivedData) | `EditText` | 原始数据显示 |
| `Button` (btnOpenClose) | `Button` | 打开/关闭串口 |

## 核心功能实现

### 1. 标签对比验证（后台API对接）

通过 `/label/getList` 接口查询扫描到的 MAC 对应的后台数据：

```java
// 分批查询：每批最多100个MAC，避免后台分页截断导致漏查
final int BATCH_SIZE = 100;
int batchCount = (beaconList.size() + BATCH_SIZE - 1) / BATCH_SIZE;

for (int batch = 0; batch < batchCount; batch++) {
    int start = batch * BATCH_SIZE;
    int end = Math.min(start + BATCH_SIZE, beaconList.size());

    StringBuilder sb = new StringBuilder();
    for (int i = start; i < end; i++) {
        if (i > start) sb.append(",");
        sb.append(beaconList.get(i).MAC);
    }

    LabelListRequest request = new LabelListRequest();
    request.setLabelCode(sb.toString()); // 逗号分隔的MAC列表
    request.setCurrent("1");
    request.setSize(String.valueOf(end - start));

    ApiService.getLabelList(request, token, new ApiCallback<LabelListResponse>() {
        @Override
        public void onSuccess(LabelListResponse response) {
            for (LabelItem item : response.getData().getResultList()) {
                for (BeaconItem beacon : beaconList) {
                    if (beacon.MAC.equalsIgnoreCase(item.getLabelCode())) {
                        beacon.hasSystemRecord = true;
                        beacon.bindCode = item.getBindCode();   // 资产编号
                        beacon.deptName = item.getDeptName();   // 所属单位
                }
            }
        }
    }
});
```

**接口返回字段说明：**

| 字段 | 说明 | 列表显示 |
|------|------|----------|
| `labelCode` | 标签编号（MAC） | MAC地址 |
| `bindCode` | 资产编号 | 资产：xxx |
| `deptName` | 所属单位名称 | 单位：xxx |
| `rssi` | 信号强度 | - |
| `elec` | 电量 | - |
| `address` | 安装地址 | - |

**分批查询说明：**
- 扫描到的 MAC 数量可能很大（数百个），后台接口通常有分页限制
- 因此将 MAC 列表分批查询，每批最多 `100` 个
- 多批次并行请求，全部完成后统一更新 UI

**统计面板与过滤：**

| 统计项 | 含义 | 点击效果 |
|--------|------|----------|
| 所有MAC | 扫描到的全部信标数量 | 显示全部，清空过滤条件 |
| 账号MAC | 后台有系统记录的信标数量 | 仅显示 `hasSystemRecord=true` 的信标，清空过滤条件 |
| 匹配资产 | 有资产编号的信标数量 | 仅显示 `bindCode != null` 的信标，清空过滤条件 |

> 点击统计项时会**自动清空 MAC/资产过滤输入框**，避免用户困惑（例如"账号MAC:28"却由于过滤条件只显示2条）。

### 2. USB串口通信 (`UsbSerialManager.java`)

```java
// 获取可用USB设备
List<UsbSerialDriver> drivers = UsbSerialProber.getDefaultProber().findAllDrivers(usbManager);

// 连接设备
serialPort.open(connection);
serialPort.setParameters(115200, 8, STOPBITS_1, PARITY_NONE);
```

**支持的USB转串口芯片：**
- FT232 (FTDI)
- CP210x (Silicon Labs)
- CH340/CH341 (WCH)
- PL2303 (Prolific)
- CDC/ACM (通用)

### 2. 数据解析逻辑

数据格式（来自G30设备）：
```
,通道,RSSI,MAC地址,数据长度,数据内容
例如: ,37,-65,00FF10ABCDEF,10,HelloWorld
```

解析代码对应关系：

**WinForm 版本：**
```csharp
string[] charactors = line.Split(strings, StringSplitOptions.None);
string chanel = charactors[2];
string RSSI = charactors[3];
string MAC = charactors[4];
```

**Android 版本：**
```java
String[] parts = line.split("[,:]");
String channel = parts[1].trim();
String rssiStr = parts[2].trim();
String mac = parts[3].trim();
```

### 3. RSSI平均值计算

**WinForm：**
```csharp
Beacons[i].RSSI = (double)(Beacons[i].RSSI * Beacons[i].ReceiveCount + int.Parse(RSSI)) / (Beacons[i].ReceiveCount + 1);
```

**Android：**
```java
public void updateRSSI(int newRSSI) {
    RSSI = (RSSI * ReceiveCount + newRSSI) / (ReceiveCount + 1);
    ReceiveCount++;
}
```

### 4. CSV导出

**WinForm：**
```csharp
string buff = "MAC,ReceiveCount,RSSI\r\n";
File.AppendAllText(FileName, buff);
```

**Android：**
```java
FileWriter writer = new FileWriter(exportFile);
writer.append("MAC,ReceiveCount,RSSI\n");
writer.append(beacon.toCsvString()).append("\n");
```

## 界面布局对应

```
┌─────────────────────────────────────────┐
│ [USB设备▼] [打开串口] [刷新]  状态:已关闭 │  ← 顶部控制栏
├─────────────────────────────────────────┤
│ [AT+STARTSCAN=NORMAL    ] [发送指令]     │  ← 命令区
├─────────────────────────────────────────┤
│ 接收到的原始数据:                          │
│ ┌─────────────────────────────────────┐ │
│ │ ,37,-65,00FF10ABCDEF,10,Hello       │ │  ← 原始数据区
│ │ ,37,-62,00FF10ABCDEG,0,             │ │
│ └─────────────────────────────────────┘ │
├─────────────────────────────────────────┤
│ [00FF10          ] [处理] [导出]         │  ← 过滤操作区
├─────────────────────────────────────────┤
│ 时间:120s  信标数:5  总数:25            │  ← 统计信息
├─────────────────────────────────────────┤
│ 资产列表 (MAC / 资产编号 / 单位 / 距离 / RSSI): │
│ ┌─────────────────────────────────────┐ │
│ │ 00FF10ABCDEF                        │ │  ← 信标列表
│ │   资产：868123088123456             │ │
│ │   单位：对外测试账号                │ │
│ │   距离：2.5m    RSSI：-64          │ │
│ │ ...                                 │ │
│ │ ...                                 │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

## 权限说明

```xml
<!-- USB通信 -->
<uses-feature android:name="android.hardware.usb.host" />
<uses-permission android:name="android.permission.USB_PERMISSION" />

<!-- 文件存储 (导出CSV) -->
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
```

## 文件结构

```
G30Scanner/
├── app/src/main/java/com/example/g30scanner/
│   ├── MainActivity.java          # 主Activity (对应Form1.cs)
│   ├── LoginActivity.java         # 登录Activity
│   ├── data/
│   │   ├── BeaconItem.java        # 信标数据类
│   │   └── model/                 # 后台API数据模型
│   │       ├── LoginRequest.java
│   │       ├── LoginResponse.java
│   │       ├── LabelListRequest.java
│   │       ├── LabelListResponse.java
│   │       └── LabelItem.java
│   ├── network/
│   │   ├── ApiClient.java         # OkHttp网络封装
│   │   ├── ApiService.java        # 业务API
│   │   └── ApiCallback.java       # 请求回调
│   ├── usb/
│   │   └── UsbSerialManager.java  # USB串口管理
│   └── utils/
│       └── TokenManager.java      # Token管理
│
├── app/src/main/res/
│   ├── layout/
│   │   ├── activity_main.xml      # 主界面布局
│   │   ├── activity_login.xml     # 登录界面布局
│   │   └── item_beacon.xml        # 列表项布局
│   ├── xml/
│   │   └── device_filter.xml      # USB设备过滤器
│   └── values/
│       ├── strings.xml            # 字符串资源
│       └── colors.xml             # 颜色定义
│
├── app/build.gradle               # 模块构建配置
├── build-debug.sh                 # 一键编译脚本
└── settings.gradle                # 项目配置
```

## 关键差异说明

| 方面 | WinForm | Android |
|------|---------|---------|
| **串口类型** | 物理COM口 | USB OTG虚拟串口 |
| **存储位置** | 用户选择任意路径 | 应用私有目录 `/Android/data/.../Exports/` |
| **UI线程** | Invoke/BeginInvoke | Handler + runOnUiThread |
| **定时器** | System.Windows.Forms.Timer | Handler.postDelayed() |
| **设备检测** | SerialPort.GetPortNames() | UsbSerialProber |

## 使用方法

1. **硬件准备**
   - G30 扫描设备
   - USB OTG 转接线
   - Android 手机（Android 5.0+）

2. **连接设备**
   - OTG线连接手机和G30设备
   - 打开App
   - 选择USB设备
   - 点击「打开串口」

3. **开始扫描**
   - 点击「发送指令」发送 `AT+STARTSCAN=NORMAL`
   - 观察信标列表实时更新

4. **导出数据**
   - 设置MAC前缀过滤（可选）
   - 点击「导出数据」
   - CSV文件保存在应用私有目录

## 调试技巧

### 查看日志
```bash
adb logcat -s G30Scanner:D
```

### 检查USB设备
```bash
adb shell lsusb
```

### 验证权限
```bash
adb shell dumpsys package com.example.g30scanner | grep permission
```

## 常见问题

1. **设备未识别**
   - 检查OTG线是否支持数据传输
   - 确认G30设备供电正常
   - 检查device_filter.xml是否包含该设备的VID/PID

2. **无法打开串口**
   - 确认已授予USB权限
   - 检查是否有其他应用占用USB设备
   - 尝试重新插拔设备

3. **数据解析错误**
   - 检查G30设备固件版本是否匹配
   - 在原始数据区查看实际接收的数据格式
   - 确认分隔符是否正确（, 和 :）
